# Demo

Open the home page, select **Load demo case**, then inspect the Evidence, Claims, Source Intel, Images, Graph, Timeline, Review, and Reports areas. `DEMO-001` is deterministic and uses fictional names, domains, and citations. It demonstrates a likely-misleading assessment caused by an image-context mismatch, a schedule conflict, unsupported attendance figure, and limited transparency—while preserving supporting evidence and an unavailable provider state.
